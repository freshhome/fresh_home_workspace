import 'package:flutter/widgets.dart';

/// Context extensions to check system accessibility settings.
extension FHReducedMotionContextExtension on BuildContext {
  /// Returns true if the user enabled "Reduce Motion" in system settings.
  bool get prefersReducedMotion {
    // SKELETON: Returns default mock state or parses media query preferences
    return false;
  }
}
